<th>
    <div class="col-10 row">
        @ColumnTitle
        @if (ColumnTitle != "Actions" && ColumnTitle != "Status" && ColumnTitle != "" && ColumnTitle != "Categories" && ColumnTitle != "Sector Types")
        {
<input class="form-control" placeholder="search" @oninput="OnSearchTextChanged" />}
    </div>
</th>

@code { [Parameter]
    public string ColumnTitle { get; set; }

    [Parameter]
    public EventCallback<ChangeEventArgs> OnSearchTextChanged { get; set; } }
